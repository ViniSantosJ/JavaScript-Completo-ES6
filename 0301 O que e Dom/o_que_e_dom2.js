// Retorne o url da página atual utilizando o objeto window

console.log('O url da página é: ', window.location.href);

// Seleciona o primeiro elemento da página que
// possua a classe ativo

console.log('O primeiro elemento com a classe "ativo" é:', document.querySelector('.ativo'));

// Retorne a linguagem do navegador

console.log('A linguagem do navegador é: ', window.navigator.language);

// Retorne a largura da janela 

console.log('A largura da janela é:', window.innerWidth, 'px');